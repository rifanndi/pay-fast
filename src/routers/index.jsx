import { createBrowserRouterh } from "react-router-dom";
import App from "../App";

export const router = createBrowserRouterh([
        {
                Path: '/',
                element:<App/>
        }
])